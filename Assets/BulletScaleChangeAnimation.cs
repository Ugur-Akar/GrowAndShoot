using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class BulletScaleChangeAnimation : MonoBehaviour
{
    //Settings
    public float scaleOscillationAmplitude;
    public float scaleOsciallationPeriod;
    // Connections
    public Transform[] scaleAbleTransforms;
    // State Variables
    
    // Start is called before the first frame update
    void Start()
    {
        //InitConnections();
        //InitState();
    }
    void InitConnections(){
    }
    void InitState(){
    }

    private void LateUpdate()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

